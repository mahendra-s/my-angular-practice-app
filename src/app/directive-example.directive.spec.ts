import { DirectiveExampleDirective } from './directive-example.directive';
import { ElementRef } from '@angular/core';
import { TemplateExampleComponent } from './template-example/template-example.component';
import { ComponentFixture, TestBed } from '@angular/core/testing';

/*

let component: TemplateExampleComponent;
let fixture: ComponentFixture<TemplateExampleComponent>;

beforeEach(async () => {
  await TestBed.configureTestingModule({
    declarations: [TemplateExampleComponent]
  })
    .compileComponents();
});

beforeEach(() => {
  fixture = TestBed.createComponent(TemplateExampleComponent);
  component = fixture.componentInstance;
  fixture.detectChanges();
});
*/

describe('DirectiveExampleDirective', () => {
  it('should create an instance', () => {
    pending('Pending for elementRef inject');
    // const element: ElementRef = fixture.elementRef;
    // const directive = new DirectiveExampleDirective(element);
    // expect(directive).toBeTruthy();
  });
});
